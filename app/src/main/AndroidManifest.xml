<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.studypoint">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".developer" />
        <activity android:name=".compilers" />
        <activity android:name=".pythonInterpreter" />
        <activity android:name=".webeditor" />
        <activity android:name=".GeeksforGeeks" />
        <activity android:name=".w3school" />
        <activity android:name=".TutorialPoint" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplasTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>